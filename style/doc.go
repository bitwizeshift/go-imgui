// package style configures the global Dear ImGui visual style and provides scoped
// style overrides.
//
// Use [Dark], [Light] or [Classic] to select a built-in preset, and [Theme] to
// read and apply the configurable layout and color options (typically once at
// startup). [Scoped] is a container widget that pushes color and variable
// overrides for the duration of its children, mirroring the begin/end pairing of
// the other container widgets.
package style
