#pragma once
// User configuration for Dear ImGui, wired in via -DIMGUI_USER_CONFIG.
//
// Intentionally minimal for v1. Compile-time overrides (for example
// IMGUI_DISABLE_OBSOLETE_FUNCTIONS, custom IM_ASSERT, or extra ImVector
// helpers) belong here as the wrapper matures. Keeping this file under our
// control means a vendored-imgui bump never clobbers our configuration.
